## Package-focused .gitignore

# Ignore the example Laravel app and its build/runtime artifacts
# We are now tracking the test app, but excluding its heavy dependencies
#/laravel-test-app/
/laravel-test-app/laravel/vendor/
/laravel-test-app/laravel/node_modules/
/laravel-test-app/laravel/storage/
/laravel-test-app/laravel/.env

# Node and Composer artifacts
/node_modules/

# Environment / secrets
/.env
/.env.*

# Build artifacts
/dist/
/build/

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Laravel-specific (for repo root we don't want to ignore package `src` or `config`)
/storage/
/bootstrap/cache/

# Logs and temp files
/logs/

# Keep composer.lock/package-lock.json tracked for reproducibility (do not ignore)
